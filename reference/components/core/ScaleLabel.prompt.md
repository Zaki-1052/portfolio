Micro "scale · magnification" eyebrow with an accent dot; use at a section's arrival and in the depth indicator.

```jsx
<ScaleLabel scale="cellular" magnification="100×" />
<ScaleLabel scale="code" dot={false} />
```

Inherits the active scale's accent from the nearest `[data-scale]` ancestor. Lowercase scale name, all-caps tracking. Keep magnification evocative, not precise.
