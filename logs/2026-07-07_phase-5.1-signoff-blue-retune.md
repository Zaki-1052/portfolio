# Phase 5.1 sign-off — verification re-run + blue-register retune

**Date:** 2026-07-07

Completes the 5.1 checkpoint that was interrupted last session, and applies a
user-directed palette revision: the third band moves from the rose register to
the home-base blue (per the master plan's vision), with rose relocated one
band down. All browser verification ran against the isolated
`chromatin-preview.html` route only.

## What was done

- **Verification re-run (isolated preview, dpr 2, all passed):**
  - Scene pass = exactly 2 meshes (beads + linkers); post passes account for
    the rest of the frame's calls.
  - Rebuild-leak: seed changes and all three presets rebuilt real new
    geometry (uuid churn confirmed — an earlier leva-driven attempt silently
    no-oped, so rebuilds were driven through the live-override channel
    imported via the Vite module graph). `gl.info.memory.geometries` flat at
    3 throughout; same-seed regen restores the bounding radius bit-exact.
  - `?drift=0` zeroes `uDriftAmp` while shimmer time advances; reduced motion
    (store flip) pins `uTime`/`uDriftAmp`/`uWaveAmp` to 0 on both materials
    and recovers cleanly.
- **Blue-register retune** (iterated by eye on the preview, then frozen):
  - `coil-params.ts`: bead base `#55404e` → `#434a56` (desaturated slate —
    material under blue light, not a dyed bead), fresnel `#d57aa5` →
    `#61afef`, linker `#8b6a9e` → `#7aa5d8` (brighter faint-glow threads).
  - `coil-bead.frag.glsl`: key light cooled `vec3(0.98,0.9,0.96)` →
    `vec3(0.9,0.95,1.0)`.
  - `ChromatinScene.tsx`: drift motes to a blue-tinted palette.
  - `coil-generator.ts`: growth defaults tightened pitch 0.55 → 0.5, beads
    96 → 106 — closes the vertical gaps between turns for the dense packed
    read (natural spacing ≈ 1.01 stays above the 0.945 floor).
- **CSS accent swap (`globals.css`):** third band → `--aod-blue` (+ soft/line
  tints, fallback `::before` tint); fourth band → `--aod-rose` (+ tints), its
  fog nudged `#243034` → `#2c2933` (rose-leaning dark; final value is a
  Phase 6 item). Dated palette-history comment updated.
- **Docs:** dated palette-revision addendum in `docs/p5-plan-coil-scene.md`.

## Decisions made

- Rejected a grayer bead base (`#3f444c`) — went murky, lost separation
  against the dark backdrop; `#434a56` keeps the luminous slate read.
- Kept the two blue bands deliberately distinct: band two electric
  (blue body + green sheath + warm motes), band three calm (slate bodies,
  single-hue accents, quiet cool motes).
- Merged-geometry bead layer stands; the `USE_INSTANCING` rewrite seam is
  untouched.

## Open items

- Live-site theming (accent/fog swap in context) to be eyeballed by the user.
- Stages 5.2+ per plan: fog module, camera knots, unwind interaction, loop
  ribbons, annotations, intro overlay, dual-register content.
- Review screenshots (`coil-baseline-*`, `coil-blue-*.png`, repo root) can be
  deleted once reviewed.

## Verification

`npm run typecheck && npm run lint && npm test -- --run` green (164 tests,
zero test edits — generator tests are parameterized against the defaults).
Production build green, preview/leva chunks excluded from `dist/`.

## Key file paths

- `src/utils/coil-generator.ts` · `src/scales/chromatin/coil-params.ts`
- `src/scales/chromatin/shaders/coil-bead.frag.glsl`
- `src/scales/chromatin/ChromatinScene.tsx` · `src/styles/globals.css`
- `docs/p5-plan-coil-scene.md`
