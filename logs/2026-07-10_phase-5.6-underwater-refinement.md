# 2026-07-10 — Phase 5.6: underwater refinement + hub-dive transition

## What was done

- **5.6a — deep-dusk re-grade.** Drums dropped from the flashbang pastel to a
  mid-tone slate-blue (`beadBaseColor #7fa3c8 → #607e9a`), ambient floor back
  down with an overhead key (light-from-above), NEW procedural env-reflection
  (reflected-ray vertical gradient, `envStrength/envDeep/envPale`), NEW
  caustic dapple on up-facing surfaces (`causticAmp/Scale`, world-anchored,
  reduced-motion static), NEW wrap-band contact shading (`wrapShadow`). The
  ring ornament's wall bands cut to a whisper (melted-stripe fix). Thread
  de-uncannied: teal biolume two-tone (`threadColor #d8b878 → #3f7d8a`, NEW
  `threadCoreColor #7fe3f2` camera-facing emissive core), real cylindrical
  shading + moving spec line, baked wrap occlusion (`aShade` attr + `threadAo`
  — static, zero unwind-tick cost), corduroy moiré killed (`sin(vT*90)` →
  slow 3-packet pulse, `threadPulseCount`). Knobs `#c2a978 → #45737f`.
- **5.6b — water medium + coherent current.** NEW pure `coil-current.ts`
  (+5 tests): one traveling plane wave (λ ≈ 105 u) shared by every layer.
  NEW `coil-atmosphere.tsx`: silt retune (650 dim small buoyant), near-field
  bokeh (DriftField + defaulted `uMaxPx`/`uRimGlow` shader extensions —
  classic fields byte-identical), NEW bubbles (points layer, wrapped-rise +
  wobble + rim-sprite shaders), veils via `CloudBank` (generalized from
  `AtmosphereClouds`, behavior-identical for tissue). Bead/thread/knob verts
  carry the identical current term (`beadCurrentAmp 0.05` — no detachment).
  NEW `coil water` leva folder + `coil-water-params` override channel;
  preview `?water=0`.
- **5.6c — hub-dive transition + band grade.** The swivel is gone: after the
  0.43 hold the camera locks onto the tree's glowing hub and passes THROUGH
  it — knots 0.442/0.454/0.464 replace the 0.455 dive (0.48/0.52/0.565
  held; reduced anchors untouched). Hub-glow swell ×4 + face widening
  (`uHubFill`, ArborMesh envelope 0.435→0.452), arbor exit fade retimed
  0.446→0.458 (fog-on-fog at the crossing; then `visible=false`), fog spike
  retimed to peak at the crossing (0.458, delta 0.02) + NEW sustained
  underwater plateau (+0.006 through the band), tint `#2b3038 → #20343a` at
  0.55. NEW `coilBloomSwellFor` in post-fx-curves (zero outside
  [0.446,0.472]). Coil reveals pushed past the crossing (lights 0.462→0.48,
  body 0.468→0.5); intro 0.465→0.487 / fade 0.498→0.52; annotations
  0.505→0.53. Band CSS: chromatin `--bg #232d31`, `--fog-color #233238`.

## Decisions made

- Camera passes THROUGH the hub (stop-at-surface = dead beat): masked by
  completed fade (faded trunk clips as fog color), glow/bloom swell, fog peak.
- Thread register: teal biolume (user choice via AskUserQuestion) — deep
  dusk water mood, glow-fill beat, ALL water elements + bubbles.
- All new values are starters on sliders, re-blessed by eye — not locks.

## Verification (previews only, dpr 2)

- Sweeps 0.40↔0.62: **60 fps both directions** (worst frame 17.8 ms).
- Stepped stills 0.43→0.565: no tree+coil co-subject frame, no clip
  artifact at the crossing, glow-fill beat present (`descent-56c-*.png`).
- Reduced motion `?depth=0.5&reduced=1`: byte-identical PNGs 2 s apart.
- Draw calls 24 mid-band / 27 at the crossing (budget ~22–24 + overlap).
- Region focus end-to-end at 0.53: label click → unwind → card → Esc path.
- Gates green ×4 stages: typecheck, lint, **208 tests** (204 + 4 net new
  files' worth), build; prod bundle free of dev/preview identifiers.
- Tissue preview regression-checked (shared mote/cloud modules extended
  with defaulted uniforms only).

## Open items

- Live-site scroll-through bless by Zara; judge the 0.452→0.462 luminance
  trough (glow → deep haze) at real scroll speed — `HUB_SWELL_*`,
  fog `FOG_COIL_*`, and `BLOOM_SWELL_*` constants are the dials.
- Thread core presence at distance (`threadEmissive 1.0`) re-judged in situ.
- Review screenshots at repo root (`coil-critique-*` = before,
  `*-56[a-d]-*` = after), deletable after review.

## Feedback round (same day, user-directed — 7 notes)

1. **Hub entry now from the TOP**: the camera crests over the canopy and
   plunges down through the crown (knots 0.442/0.454/0.464 re-authored;
   plunge line biased forward through the sphere's glowing front-top
   quadrant), and a new `uHubFill²`-driven **glow spill (×0.75)** ignites
   the whole structure at the fill peak so limbs crossing the orb read as
   luminous shapes, never dark eclipses.
2. **Twinkle is back**: new `sparkle` layer (150 pts, old palette/size,
   NEW defaulted `uTwinkle` per-point pulse in the shared mote shader) +
   `sparkleCount/Opacity` sliders.
3. **Normal POV main view**: 0.52/0.565 knots raised to the coil's
   shoulder (camera y −22/−23 vs the old −30) gazing gently DOWN; 0.48
   target aimed high so the column sits low in frame; intro sooner/shorter
   (0.462→0.478 / 0.49→0.51); annotations 0.5→0.525.
4. Trough dials documented for the user (see PLAN.md status note).
5. **Thread face-on fix**: THREAD_RADIAL 6→8 (dark-rail split), radius
   0.055→0.07 (chunkier), body `#3f7d8a→#4c8d9b` (near-wall value — a
   much darker cord read as cracks cut INTO the wall), core floor 0.65 /
   lobe pow 1.3.
6. **Card legibility**: deeper/larger backdrop pool (0.82 core), meta text
   brightened (#eef2f7 / #ccd7e2 / #b9c5d3).
7. **Focus pose de-scared**: REGION_FOCUS_LIFT 2→6, DISTANCE 20→19 — the
   opened region lays out below the lens; silt shell rOuter 20→26 keeps
   particles in frame at focus poses.

Re-verified: 60 fps sweep, 208 tests, build clean.

## Feedback round 2 (external feedback via user — 5 notes on the modeling)

1. **Puck, not coin**: caps are now shallow paraboloid CROWNS (`beadDome`
   0.1 · slider 0–0.25, analytic normals in `buildBeadTemplate`) with a
   bigger bevel (0.14→0.2); NEW `radiusJitter` growth param (0.12, seeded
   after the offsets stream so positions held per seed) breaks the
   stamped-uniform read.
2. **Visibly wound**: `wrapTurns` 1.6→2.6 (slider max 3.5) spread across a
   widened wall band (`WRAP_Z_FRACTION` 0.55→0.62; `WRAP_SAMPLES` 28→44 to
   stay round) — each drum now carries an unmistakable multi-turn wound
   band; adjacency AO grooves the touching turns.
3. **Face-on "falling apart"**: resolved by 2 + 4 — the turns read as
   physical windings, the opened pitch turns crack-slits into deliberate
   gaps.
4. **Looser packing**: `coilPitch` 1.05→1.18, `jitter` 0.08→0.12.
5. **Net → spiral**: cap ornament is now a seam-free wound SPIRAL
   (angular coefficient exactly 1 across the atan cut), `ringAmp` 0.45→0.3,
   `ringFreq` 5→4, wall banding whisper 0.3→0.2.

Also this round (user): publication cards enlarged (label 15px, body
380px/15px text, near-white two steps brighter, deeper backdrop pool) and
the intro stepped DOWN one type-scale rank (title 3xl→2xl, prose md→base) —
the cards outrank the intro now. Gates + 60 fps sweep re-verified.

## Key file paths

- `src/scales/chromatin/`: `coil-current.ts(+test)` NEW ·
  `coil-atmosphere.tsx` NEW · `coil-water-params.ts` NEW ·
  `shaders/coil-bubbles.{vert,frag}.glsl` NEW · `coil-params`, 
  `coil-materials`, `coil-geometry`, `CoilMesh`, `ChromatinScene`,
  `CoilIntro`, `CoilAnnotations`, `coil-fog(+test)`, bead/thread/knob shaders
- `src/scales/tissue/atmosphere-motes.tsx(+shaders)`,
  `atmosphere-clouds.tsx` (→ CloudBank) ·
  `src/scales/cellular/ArborMesh.tsx`, `shaders/arbor-trunk.frag.glsl`,
  `arbor-trunk-material.ts`
- `src/engine/camera-keyframes.ts`, `post-fx-curves.ts(+test)` ·
  `src/dev/coil-dev-tools.tsx`, `chromatin-preview.tsx` ·
  `src/styles/globals.css` · `PLAN.md` · `docs/p5-plan-coil-scene.md`
